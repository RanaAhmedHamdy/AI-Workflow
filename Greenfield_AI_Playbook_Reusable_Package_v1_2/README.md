# Adaptive UI Reusable Update Package v1.0

Adds an `adaptive-compose` skill, stronger Android AGENTS overlays, adaptive checks in PR and architecture reviews, an expanded architecture checklist, and vending-machine integration snippets.

Skills explain how. `AGENTS.md` makes the requirement mandatory. Architecture documents define the project-specific pattern. Reviews, tests, and runtime evidence verify compliance.
